import { render } from '@testing-library/react';
import React from 'react';

const Logo = (props) => {

    return(
      <div className="logo">
        <p>Hi I'm logo</p>
      </div>
    )
  }

export default Logo;